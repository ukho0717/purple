import React from 'react';
import EnterMain from '../../components/enter/EnterMain';
import { withRouter } from 'react-router-dom';

const EnterMainContainer = () => {
    return (
        <EnterMain/>
    )
}

export default withRouter(EnterMainContainer);