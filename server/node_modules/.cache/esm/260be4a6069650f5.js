let User,mongoose,Joi;_069‍.x([["list",()=>list],["update",()=>update]]);_069‍.w("../../models/user",[["default",["User"],function(v){User=v}]]);_069‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_069‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);



const { ObjectId } = mongoose.Types;

       const list = async ctx => {
    ctx.body = '결제목록'
};

       const update = async ctx => {
    console.log('/payment update 호출');

    const _id = ctx.state.user._id;

    const schema = Joi.object().keys({
        premium: Joi.string().required()
    });

    const result = schema.validate(ctx.request.body);
    if(result.error){
        ctx.status = 400;
        ctx.body = result.error;
        return;
    }

    try{
        const post = await User.findByIdAndUpdate(_id, ctx.request.body, {
            new: true
        }).exec();
        if(!post){
            ctx.status = 404;
            return;
        }
        ctx.body = post;
    }catch(e){
        ctx.throw(500, e);
    }
}