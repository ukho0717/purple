let User,mongoose,Joi;_76e‍.x([["list",()=>list],["update",()=>update]]);_76e‍.w("../../models/user",[["default",["User"],function(v){User=v}]]);_76e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_76e‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);



const { ObjectId } = mongoose.Types;

       const list = async ctx => {
    ctx.body = '결제목록'
};

       const update = async ctx => {
    const { id } = ctx.params;
    // console.log(id);
    // console.log(ctx.request.body);

    const schema = Joi.object().keys({
        premium: Joi.string().required()
    });

    const result = schema.validate(ctx.request.body);
    if(result.error){
        ctx.status = 400;
        ctx.body = result.error;
        return;
    }
    
    try{
        const post = await User.findByIdAndUpdate(id, ctx.request.body, {
            new: true
        }).exec();
        if(!post){
            ctx.status = 404;
            return;
        }
        ctx.body = post;
    }catch(e){
        ctx.throw(500, e);
    }
}